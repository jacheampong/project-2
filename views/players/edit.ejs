<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Player Edit Page</title>
</head>
<body>
    <div>
        <h1>Player Edit Page</h1>
        <form action="/players/<%=player.id%>?_method=PUT" method="POST" >
            <fieldset>
                <legend>Player Profile</legend>
                <label for="name">Name: </label>
                <input type="text" id="name" name="name" required value="<%=player.name%>" /> <br>
                <label for="position">Position: </label>
                <select name="position" id="position" required  >
                    <option value="">--Select a position--</option>
                    <option 
                        value="goalkeeper"
                        <% if (player.position === "goalkeeper") { %>
                            selected
                        <% } %> >Goal keeper</option>
                    <option 
                        value="defender"
                        <% if (player.position === "defender") { %>
                            selected
                        <% } %> >Defender</option>
                    <option 
                        value="midfielder"
                        <% if (player.position === "midfielder") { %>
                            selected
                        <% } %> >Midfielder</option>
                    <option 
                        value="attacker"
                        <% if (player.position === "attacker") { %>
                            selected
                        <% } %> >Attacker</option>
                </select> 
                <br>
                <label for="jersey">Jersey #: </label>
                <input type="text" id="jersey" name="jersey" value="<%= player.jersey %>" /> <br>
                <label for="team">Team: </label>
                <input type="text" id="team" name="team" value="<%= player.team %>" /> <br>
                <label for="isActive">Active</label>
                <input 
                    type="checkbox" 
                    id="isActive" 
                    name="isActive"
                    <% if (player.isActive === true) { %>
                        checked
                    <% } %> 
                 /> <br>

                <input type="submit" value="Submit">
            </fieldset>
        </form>
        <br>
        <button type="button" name="button">
            <a href="/players/<%=player.id%>">Back</a>
        </button>

    </div>
    
    
</body>
</html>