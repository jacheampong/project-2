<!DOCTYPE html>
<html lang="en">
<head>
    <%- include ('../partials/head.ejs') %>
</head>
<body>
    <div class="player-profile">
        <fieldset>
            <legend><%= player.name %>'s Profile</legend>
            <img class="player-image" src="../images/<%= player.image %>.jpg" alt="<%= player.name %>"> <br>
            Name: <%= player.name %> <br>
            Position: <%= player.position %> <br>
            Jersey No.: <%= player.jersey %> <br>
            Team: <%= player.team ? player.team : 'Not assigned' %> <br>
            Active: <% if(player.isActive === true) { %>
                 ✓ 
            <% } else { %> 
                ✕
            <% } %> 
        </fieldset>
        <p>
            <button type="button" name="button"><a href="/players/<%=player._id%>/edit">Edit</a></button>
            <form action="/players/<%= player._id %>?_method=DELETE" method="POST">
                <input type="submit" value="DELETE"/>
            </form>
        </p>
        
        <!-- <br> -->
        <button type="button" name="button">
            <a href="/players">Back</a>
        </button>
        <!-- <br> -->
        <button type="button" name="button">
            <a href="/">Home</a>
        </button>
    </div>
    
</body>
</html>